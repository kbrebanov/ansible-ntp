---
# defaults file for ntp

ntp_servers:
  - 0.{{ ansible_distribution|lower }}.pool.ntp.org
  - 1.{{ ansible_distribution|lower }}.pool.ntp.org
  - 2.{{ ansible_distribution|lower }}.pool.ntp.org
  - 3.{{ ansible_distribution|lower }}.pool.ntp.org
